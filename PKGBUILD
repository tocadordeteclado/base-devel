#
# Contribuidor: {{ nome_do_autor(); }}
#

pkgname=base-devel
pkgver=1
pkgrel=1
pkgdesc='Ferramentas básicas para construir programas do sistema'
url='http://localhost/base-devel'
arch=('any')
license=('cIO')
options=('!debug')
depends=(
    archlinux-keyring
    autoconf
    automake
    binutils
    bison
    debugedit
    fakeroot
    file
    findutils
    flex
    gawk
    gcc
    gettext
    grep
    groff
    gzip
    libtool
    m4
    make
    pacman
    patch
    pkgconf
    sed
    sudo
    texinfo
    which
)
